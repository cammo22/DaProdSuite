{
  "name": "@daprod/gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Gateway HTTP della suite: accoppiamento QR/codice, dispositivi, richieste remote, notifiche.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@daprod/azioni": "workspace:*"
  }
}
