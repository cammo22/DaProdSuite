/**
 * Quello che più moduli devono poter guardare.
 *
 * Serve perché la scheda Crea mostra le ultime immagini fatte, che sono le
 * stesse della Galleria: due elenchi separati vorrebbero dire vederne una in un
 * posto e non nell'altro.
 */

export const stato = {
  /** Le immagini in libreria, dalla più recente. */
  immagini: [],
};
