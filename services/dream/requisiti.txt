# Le librerie che servono a DaProdDream e che l'ambiente condiviso non ha già.
#
# Torch NON si tocca: c'è già con la build CUDA giusta, e chi lo rimette può
# solo sostituirlo con una wheel peggiore. Vale per tutti i motori della suite
# (packages/runtime/src/requisiti.ts).
#
# Chi manca davvero rispetto a ComfyUI: diffusers (la pipeline SD-Turbo), la
# cattura dello schermo, e il video delle registrazioni.
#
# ─────────────────────────────────────────────────────────────────────────────
# I NUMERI ESATTI NON SONO PIÙ QUI.
#
# Fino al 19 agosto 2026 questo file aveva solo minimi (`>=`), con accanto
# scritto che i minimi bastavano perché «a rompere l'ambiente non sono gli
# aggiornamenti, sono le retrocessioni». Era vero a metà: un minimo toglie il
# rimbalzo fra due nostre liste, ma non impedisce a ComfyUI o a un nodo custom
# di tirare dentro l'ultima `diffusers` uscita stamattina — e quella libreria la
# usano anche Foto, IoDigitale e il Companion.
#
# Adesso i numeri stanno in un posto solo, `packages/runtime/requirements/
# versioni.txt`, che viene passato come **vincolo** a ogni installazione della
# suite, comprese quelle del codice di terzi. Qui resta solo il minimo che dice
# «sotto questa versione il motore non funziona», che è un'informazione diversa
# e che vale la pena avere scritta accanto al pacchetto.
# ─────────────────────────────────────────────────────────────────────────────

diffusers>=0.38.0
accelerate>=1.10.0
peft>=0.19.1

fastapi>=0.121.0
uvicorn>=0.38.0
websockets>=15.0
python-multipart>=0.0.20

opencv-python>=4.11.0
mss>=10.0.0
imageio-ffmpeg>=0.6.0
nvidia-ml-py>=13.0
