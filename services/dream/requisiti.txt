# Le librerie che servono a DaProdDream e che l'ambiente condiviso non ha già.
#
# Torch NON si tocca: c'è già con la build CUDA giusta, e chi lo rimette può
# solo sostituirlo con una wheel peggiore. Vale per tutti i motori della suite
# (packages/runtime/src/requisiti.ts).
#
# Chi manca davvero rispetto a ComfyUI: diffusers (la pipeline SD-Turbo), la
# cattura dello schermo, e il video delle registrazioni.

# **I minimi sono alzati alle versioni provate il 19 agosto 2026**, e non è
# pignoleria: a rompere l'ambiente non sono gli aggiornamenti, sono le
# **retrocessioni**. Quella notte `huggingface-hub` e `transformers` sono
# rimbalzate avanti e indietro fra due installazioni finché una disinstallazione
# non è fallita a metà (l'errore 4395 dell'antivirus, vedi `uv.ts`), e Musica,
# Foto, Dream e IoDigitale hanno smesso di funzionare tutte insieme. Un minimo
# non impone niente a nessuno — al contrario di `==`, che è il motivo per cui
# qui sopra c'è scritto di non usarlo — ma toglie di mezzo il rimbalzo.
diffusers>=0.38.0
accelerate>=1.10.0
peft>=0.19.1

fastapi>=0.121.0
uvicorn>=0.38.0
websockets>=15.0
python-multipart>=0.0.20

opencv-python>=4.11.0
mss>=10.0.0
imageio-ffmpeg>=0.6.0
nvidia-ml-py>=13.0
